# Task #1768 — Phase 2 verification (tenancy enforcement + SEMrush)

Covers Stage 3 (tenancy) and Stage 4 (SEMrush switches one at a time).
Each sub-section is filled in by the operator after the matching
60-minute window.

```
Phase: 2 — Tenancy + SEMrush (Pool Epic § 2 + § 3)

Sub-phase 2a — db_pool_tenancy_enforcement_enabled

  Activation timestamp:  <FILL>
  Verification window:   activation+0 → activation+60 min, then re-check at +24h

  Baseline (from 1768-baseline.md):
    - API pool 1h avg util:               91.8 %
    - API pool 1h max waiters:            34
    - Worker pool 1h avg util:            65.4 %
    - Worker pool 1h max waiters:         2
    - API pool top-5 by maxMs contains 3 worker:* labels:
        * worker:semrush_background_refresh:enrich_campaigns (16 463, 101 053 ms)
        * worker:google-drive-sync                            (103, 84 229 ms)
        * worker:retroactive_reprocess (7d window, API)       (302, 402 100 ms running-top-N)

  Post-flip (+60 min):
    - API pool avg util:                  <FILL>
    - API pool max waiters:               <FILL>
    - Worker pool avg util:               <FILL>
    - Worker pool max waiters:            <FILL>
    - API pool top-5 by maxMs still contains any worker:* label? <FILL: yes/no + which>
    - stale_lease_exhaustion spike since flip? <FILL: counts per queue>

  Post-flip (+24 h):
    - API pool 24h avg util:              <FILL>
    - API pool 24h max waiters:           <FILL>
    - Worker pool 24h avg util:           <FILL>
    - Worker pool 24h max waiters:        <FILL>
    - notifyUser broadcast latency spike? <FILL>
    - Front recovery throughput change?   <FILL>

  Success metric verdict:
    PASS only if (a) API pool 1h avg util drops materially below the 91.8 %
    baseline, AND (b) worker pool sustains <80 % util, AND (c) no worker:*
    label remains in the API pool top-5 by maxMs after a full 60 min window.

  Issues found:           <FILL or "none">
  Follow-up tasks filed:  <FILL or "none">
  Rollback trigger:       worker pool >80 % sustained, worker waiters >10,
                          stale_lease_exhaustion spike, Front recovery slows.
  Rollback status:        not triggered / triggered → <reason + timestamp>

----

Sub-phase 2b — semrush_persistent_enrichment_cache_enabled

  Activation timestamp:  <FILL>
  Verification window:   activation+0 → activation+60 min (or one full SEMrush refresh cycle)

  Baseline:
    - semrush_background_refresh completed (24h):  105
    - external SEMrush call count last 24h:        n/a (audit was off)
    - external SEMrush cache_hit_pct last 24h:     n/a
    - worker:semrush_background_refresh:enrich_campaigns total count:
        16 463 (running-top-N up to 2026-05-22 16:00 UTC)

  Post-flip (+60 min):
    - external SEMrush calls last 60 min:          <FILL from external_call_audits>
    - external SEMrush cache_hit_pct last 60 min:  <FILL>
    - same_response_pct last 60 min:               <FILL>
    - worker:semrush_background_refresh:enrich_campaigns count delta: <FILL>

  Success metric verdict:
    PASS if enrich_campaigns call count drops materially (target ≥ 50 %),
    cache_hit_pct > 0, no SEMrush error spike.

  Issues found / follow-ups / rollback status: <FILL>

----

Sub-phase 2c — semrush_no_external_calls_inside_db_hold_enabled

  Activation timestamp:  <FILL>
  Verification window:   activation+0 → activation+60 min (or one SEMrush apply cycle)

  Baseline:
    - semrush_heatmap_apply pending:               244
    - semrush_heatmap_apply:apply max hold (rollup): <FILL after Stage 1>
    - semrush_heatmap_apply dead_letter (7d):      0 in current snapshot

  Post-flip (+60 min):
    - semrush_heatmap_apply pending:               <FILL>
    - semrush_heatmap_apply completed since flip:  <FILL>
    - any SEMrush hold label still > 10 000 ms in db_hold_label_rollups? <FILL>
    - semrush_heatmap_apply dead_letter delta:     <FILL>

  Success metric verdict:
    PASS if no SEMrush hold label exceeds 10 s in the post-flip window,
    and heatmap apply continues to drain.

  Issues found / follow-ups / rollback status: <FILL>
```

## Required manual QA after Phase 2

- Re-run `notifyUser()` end-to-end via the test notification path; the
  bell still updates. (Same as Phase 1 QA, repeated because tenancy
  enforcement routes `notifyUser()` worker-context callers onto
  `workerDb`.)
- Visit a SEMrush-backed dashboard; confirm data still renders.
- Hit `/admin/db-attribution/trends` "background on API pool" panel —
  it should be empty (or only contain documented exceptions).
