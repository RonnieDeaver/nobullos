# Task #3786: the agent memory index is append-only by convention; union
# merge keeps both sides' appended bullets instead of producing a whole-file
# conflict. Worst case is a duplicated bullet, which is cheap to spot.
# (The former LFS rule for an attached_assets PDF was removed along with its
# file in the Task #3790 repo-weight cleanup.)
.agents/memory/MEMORY.md merge=union
attached_assets/Revenue_Engine_Sales_Deck___July_13,_2026_(2)_1785952349453.pptx filter=lfs diff=lfs merge=lfs -text
attached_assets/physical-book-cover-create-a-b_1787601394861.zip filter=lfs diff=lfs merge=lfs -text
